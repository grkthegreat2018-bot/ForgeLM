# PyTorch wheels for CUDA 13.0 (Blackwell / RTX 5070, sm_120)
# PyTorch 2.13: FlexAttention FA4, CuTeDSL Inductor, LinearCrossEntropyLoss
--extra-index-url https://download.pytorch.org/whl/cu130
torch==2.13.0+cu130
numpy>=2.2,<2.5  # 2.4.x tested with torch 2.13; 2.5+ may break ABI

# Hugging Face stack (from PyPI)
# transformers pulls in tokenizers, huggingface_hub, accelerate as transitive deps.
datasets==4.5.0
transformers==4.51.3

# Fast BPE tokenizer (~1000x faster than HF tokenizers on bulk encode).
# Used by research.prep_data when --tokenizer-backend gigatoken-* is set.
# Optional: prep_data falls back to HF AutoTokenizer if this is absent and
# the backend is `hf` (the default).
gigatoken==0.9.0

# 8-bit/4-bit optimizer support — now available on Windows with Blackwell (sm120) builds.
# Falls back to torch.optim.AdamW if absent.
bitsandbytes==0.49.2

# Triton for torch.compile on Windows (patched at runtime by research.training_utils).
triton-windows==3.4.0.post21

# Utilities
python-dotenv==1.0.1

# Explicit deps (previously transitive — now declared for reliability)
safetensors>=0.4.0

# Performance: JSON serialization (5-10x faster than stdlib json)
msgspec>=0.21

# DX: beautiful tracebacks + progress bars
rich>=14.0

# DX: structured logging (replaces print statements, staged migration)
loguru>=0.7.3

# Quantization: int4 weight-only for 58% VRAM reduction
torchao>=0.17

# Serving (optional — only needed for chat_ui.py)
# gradio>=4.0  # Uncomment if using research/serving/chat_ui.py

# Dev / testing (install with: pip install -e ".[dev]")
# pytest>=8.0
# pytest-cov>=5.0
# ruff>=0.16
