"""ForgeAI research package — experimental scripts and porting tools only.

Production code has been migrated to the forge/ package. This package
retains only experimental/porting code:
    architecture    — MTP heads, LFM2.5 porting reference
    vision          — V10 to V11 porting
    training_free   — bake, solver, steering (experimental)
    cloud           — vast_connector (experimental cloud training)
"""
from __future__ import annotations
