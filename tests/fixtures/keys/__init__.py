"""Test-only key fixtures — moved from forge/keys/ (dead key cleanup F9).

These key files are not imported by forge/model_loader.py. They are preserved
here because tests and scripts still reference them.
"""
